import 'package:get_it/get_it.dart';

// Create a service locator instance
final sl = GetIt.instance;

// For now, the init function is empty.
// You will register your dependencies here later.
Future<void> init() async {
  // BLoCs, Use Cases, Repositories, and Data Sources will be registered here
  // as you build them.
}
